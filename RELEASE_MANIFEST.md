# 发布候选包清单

本目录是从本地运行系统中剥离出的独立发布候选包，原系统未被删除、移动或覆盖。

## 已包含

- 门户服务、前端源码、测试和启动/停止脚本。
- OCR 所需中英文语言数据。
- 运行必需的 `memory-graph` MIT 源码。
- 已构建的 `www/` 前端预览产物，用于克隆后直接启动；其中 `www/cache/` 仍不提交。
- README、操作手册、架构决策、发布审阅清单、许可证与第三方声明。

## 明确未包含

- Obsidian Vault、电子书、日记、笔记、照片、导出包、图谱缓存、状态、日志、暂存和备份。
- `.env`、Obsidian API 密钥、任何私有配置和全部 `.git` 历史。
- 个人迁移/分流规则、原系统的审查报告、`frontend/node_modules`、Python 虚拟环境和运行缓存。
- 未复核授权来源的背景图片；候选版改用纯 CSS 环境背景。

## 可移植性改造

原有机器专属目录已替换为 `BRAIN_WEB_VAULT`、`BRAIN_WEB_PYTHON` 等运行时环境变量。前端本地 `memory-graph` 依赖已移入 `tools/memory-graph`，不再依赖仓库外目录。
