# 更新记录

本文件记录可审阅的发布候选包变更。当前尚未发布，也尚未建立 Git 历史。

## [Unreleased]

### 新增

- 独立的脱敏发布候选包、发布卫生检查、操作手册、架构说明、发布审阅清单和 ADR。
- `.env.example`、Python 依赖清单、第三方组件声明与候选许可证占位。
- 面向仓库外 Vault、Cookie 和 API 密钥的运行时配置接口。

### 变更

- 移除特定 Vault 路径、个人背景图、运行缓存、日志、导出包、备份、虚拟环境、Cookie、配置与嵌套 Git 历史。
- 前端改为纯 CSS 背景，不再再分发原有图片资产。
- 依赖锁定文件按当前发布候选依赖重新生成；安全升级仅在构建与测试通过后保留。

### 注意

- 公开发布前必须由维护者选择许可证、复核 OCR 语言数据来源，并完成 `docs/发布前审阅清单.md`。
