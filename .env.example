# 复制为 .env 后按需填写。不要提交 .env。
# 可选：Obsidian Vault 的绝对路径，且应位于仓库外。
# 不填写或路径不存在时，启动门户.bat 会自动创建 demo_vault 供预览。
# BRAIN_WEB_VAULT=C:\Users\YOUR_NAME\Documents\My-Obsidian-Vault

# 可选：门户端口。默认 8765；占用时启动脚本会自动尝试后续端口。
# BRAIN_WEB_PORTAL_PORT=8765

# 可选：发布候选版默认不启用门户访问口令，便于本机一键预览。
# 如需恢复口令保护，同时填写下面两项，token 至少 16 位。
# BRAIN_WEB_PORTAL_AUTH=1
# BRAIN_WEB_PORTAL_TOKEN=请设置至少16位随机口令

# 可选：基础门户预览不需要安装 Python 第三方依赖。
# 需要屏幕框选 OCR 或 REST 验证脚本时，可显式开启安装。
# BRAIN_WEB_INSTALL_OPTIONAL_DEPS=1

# 可选：Obsidian Local REST API 的密钥仅供命令行验证脚本使用。
# BRAIN_WEB_OBSIDIAN_API_KEY=
# BRAIN_WEB_OBSIDIAN_API_URL=http://127.0.0.1:27123

# 可选：任务计划程序可显式指定 Python；日常命令默认使用当前 Python。
# BRAIN_WEB_PYTHON=C:\path\to\python.exe
